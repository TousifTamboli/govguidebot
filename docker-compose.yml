version: "3.9"

services:
  python-app:
    build: .
    container_name: python_app
    volumes:
      - .:/app
    ports:
      - "5001:5000"   # change if needed
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped